# Text Input

Generate a text input field using React and Tailwind CSS.

## Prompt

Create a React text input component that:

- Is styled for a dark theme interface
- Has appropriate placeholder styling
- Includes focus state with visible indicator
- Supports an "invalid" error state
- Is appropriately sized for a data-dense interface

Output only the input code, no explanations.
