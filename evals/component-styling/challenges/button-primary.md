# Button Primary

Generate a primary action button using React and Tailwind CSS.

## Prompt

Create a React button component that serves as the main call-to-action. It should:

- Be styled for a dark theme interface
- Have a prominent, clickable appearance
- Include hover and focus states
- Be appropriately sized for a data-dense interface

Output only the button code, no explanations.
