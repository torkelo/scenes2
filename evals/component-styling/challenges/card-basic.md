# Basic Card

Generate a card container using React and Tailwind CSS.

## Prompt

Create a React card component that:

- Has a header section with a title
- Has a content section
- Is styled for a dark theme interface
- Has appropriate borders and background
- Uses compact spacing suitable for data-dense interfaces

Output only the card code, no explanations.
