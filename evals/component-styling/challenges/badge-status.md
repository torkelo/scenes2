# Status Badge

Generate status badges using React and Tailwind CSS.

## Prompt

Create React badge components that indicate status. Include variants for:

- Success (healthy, active)
- Warning (degraded, pending)
- Error (failed, critical)
- Info (informational)

Style them for a dark theme interface with appropriate colors for each status.

Output only the badge code, no explanations.
