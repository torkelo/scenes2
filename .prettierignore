node_modules
dist
.turbo
pnpm-lock.yaml
CHANGELOG.md
*.hbs

# Generated by apps/design-site/scripts/build-search-index.js (prebuild hook)
apps/design-site/public/search-index.json

# Generated by scripts/importIcons.ts; the script formats each file via prettier
packages/icons/src/components/Icons
.pnpm-store

# Local scratch area (git-ignored via .git/info/exclude); the whole-tree check
# in the pre-push hook and CI must not trip over uncommitted notes.
.context

# Generated scenario eval builds (git-ignored via apps/storybook/scenarios/.gitignore);
# same reason — the whole-tree check must not trip over these uncommitted artifacts.
apps/storybook/scenarios/*/v*/
apps/storybook/scenarios/*/v*.stories.tsx

# Claude Code worktrees are independent checkouts with their own generated
# files; the whole-tree check must not walk into them.
.claude/worktrees
